Reject image pulls when Docker reports a structured stream error, emits malformed JSON or UTF-8, or ends with a final nonblank frame other than its terminal downloaded-newer or already-up-to-date result. Untagged pull names now explicitly request `latest` so Docker does not interpret them as all-tags pulls. Pull progress remains newline-delimited and streaming across arbitrary response chunk boundaries.
