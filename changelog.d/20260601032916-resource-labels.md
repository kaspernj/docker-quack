- Support `Labels` on container and network `create` (typedefs document the existing pass-through) and on container `commit` (sent as the commit container config) so created/committed Docker resources can carry tracking labels.
