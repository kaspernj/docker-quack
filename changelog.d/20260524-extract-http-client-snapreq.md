Extract the HTTP client plumbing into the cross-platform `snapreq` library. `DockerConnection` now delegates connection handling, Unix sockets, client TLS, request/response compression and streaming to `snapreq`, while keeping the Docker-specific behaviour (default `Accept-Encoding`, JSON/text response parsing, `DockerApiError`, and retrying transient Docker API / connection failures). No change to the public Docker API.
